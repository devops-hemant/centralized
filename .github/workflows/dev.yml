# This is a basic workflow to help you get started with Actions

name: DEV

# Controls when the workflow will run
on:
  workflow_call:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Hello, world! from DEV"

      - name: Checkout
        uses: actions/checkout@v3
        with:
           fetch-depth: 0
      - name: Get changes
        run: |
          set -x
          filenames=$(git diff --name-only ${{ github.event.before }} ${{ github.event.after }})

          # echo "#######################################################"
          # echo "$allnames"
          # echo "#######################################################"

          # filenames=$(echo $allnames | find . -type f \( -name "*.py" -or -name "*.scala" -or -name "*.sql" -or -name "*.r" -or -name "*.html" -or -name "*.dbc" -or -name "*.ipynb" -or -name "*.Rmd" \))

          echo "#######################################################"
          echo "$filenames"
          echo "#######################################################"

          while read -r file; do
            echo -e "$file"
            if [[ "$file" = *.@(py|scala|sql|r|html|dbc|ipynb|Rmd) ]];
            then
              echo "file is $i"
            else
              echo "file is not available"
            fi
          done < $filenames

          # for i in $filenames
          # do
          #   if [[ "$file" = *.@(py|scala|sql|r|html|dbc|ipynb|Rmd) ]];
          #   then
          #     echo "file is $i"
          #   else
          #     echo "file is not available"
          #   fi
          # done